[project]
name = "readpapermachine"
version = "0.1.0"
description = "AI-assisted research knowledge management system"
requires-python = ">=3.10"

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
]
